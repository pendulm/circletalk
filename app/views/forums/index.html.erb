<h1>Listing forums</h1>

<% Forum.build_tree.each do |forum, l| %>
  <p>
  <%= "|-" * l %>
  <%= "#{forum.id}" %>
  <%= link_to forum.name, forum %>
  <%= link_to 'Edit', edit_forum_path(forum) %>
  <%= link_to 'Destroy', forum, :confirm => 'Are you sure?', :method => :delete %>
  </p>
<% end %>

<br />

<%= link_to 'New Forum', new_forum_path %>
